const Base64ToHex = require('./src/base64-to-hex')
const HexToBase64 = require('./src/hex-to-base64')
const ToBin = require('./src/to-bin')
const ToGUID = require('./src/to-GUID')
const IsGUID = require('./src/is-guid')

module.exports = {
  Base64ToHex,
  HexToBase64,
  ToBin,
  ToGUID,
  IsGUID
}
